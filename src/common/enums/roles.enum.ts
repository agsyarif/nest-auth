export enum Role {
  SuperAdmin = 'super admin',
  Admin = 'admin',
  Customer = 'customer',
  Member = 'member',
}

export enum RoleId {
  SuperAdmin = 1,
  Admin = 2,
  Customer = 3,
  Member = 4,
}